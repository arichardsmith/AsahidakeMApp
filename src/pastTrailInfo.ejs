<!doctype html>
<!-- このデータは自動作成、編集は../src/pastTrailInfo.ejsで-->
<html lang="ja">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">

        <meta name="viewport" content="initial-scale=1,user-scalable=no,minimum-scale=1, maximum-scale=1, width=device-width">
        <!--最小限のビューポート設定-->
        <meta name="viewport" content="width=device-width">

        <meta name="mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-capable" content="yes">
         <link rel="stylesheet" href="resources/bulma.css" />

	</head>
    <body>
		<h3 class="title is-3">過去の登山情報</h3>
		<p>
			各時期の登山道の参考情報(2020のデータ)　最新データはホームページから見ることが出来ます。
		</p>

		<br><p class="is-size-5">旭岳山頂へ登山道</p>
		<% for (let entry of tozanJoho.summit) { %>
		<a  href="<%= entry.src %>" target="_blank" rel="noopener noreferrer">
			<p><%= entry.displayDate %></p>
		</a>
		<% } %>


		<br><p class="is-size-5">天女ヶ原</p>
		<% for (let entry of tozanJoho.tennyogahara) { %>
			<a  href="<%= entry.src %>" target="_blank" rel="noopener noreferrer">
				<p><%= entry.displayDate %></p>
			</a>
			<% } %>

		<br><p class="is-size-5">裾合平</hp>
			<% for (let entry of tozanJoho.nakadake) { %>
				<a  href="<%= entry.src %>" target="_blank" rel="noopener noreferrer">
					<p><%= entry.displayDate %></p>
				</a>
				<% } %>

		<br><p class="is-size-5">裏旭岳キャンプ場</hp>
			<% for (let entry of tozanJoho.uraasahi) { %>
				<a  href="<%= entry.src %>" target="_blank" rel="noopener noreferrer">
					<p><%= entry.displayDate %></p>
				</a>
				<% } %>


	</body>
</html>